export enum EPages {
    Home = 'home',
    Complexes = 'complexes',
    Posts = 'posts',
    NewPost = 'new-post',
    SignIn = 'sign-in',
}

export enum EMenuItemsVisibility {
    Always = 'always',
    GuestOnly = 'guest-only',
    AuthOnly = 'auth-only',
}
